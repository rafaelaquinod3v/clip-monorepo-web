{
  "name": "desktop-legacy",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/desktop-legacy/src",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx electron apps/desktop-legacy/src/main.js"
      }
    },
    "package": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx electron-forge package",
        "cwd": "apps/desktop-legacy"
      }
    },
    "make": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx electron-forge make",
        "cwd": "apps/desktop-legacy"
      }
    }
  }
}
